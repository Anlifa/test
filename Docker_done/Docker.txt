1.apt update
2.apt install apt-transport-https ca-certificates curl gnupg2 software-properties-common
3.curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add -
4.add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/debian $(lsb_release -cs) stable"
5.apt update
6.apt-cache policy docker-ce
7.apt install docker-ce
8.systemctl status docker
9.mkdir anna && cd anna && nano Dockerfile

FROM node:latest
WORKDIR /anna
RUN apt-get update && \
apt-get install -y nginx
COPY tpt/docker/javascript/ ./
RUN npm install && npm run ng build -- --output-path=/var/www/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]

10.git clone https://github.com/pannoi/tpt.git
11.docker build -t liisa .
12.docker run -it -p 80:80 liisa
13.Firefox -> localhost
